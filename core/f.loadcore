#!/bin/bash

# Use `source f.loadcore` to load the core commands.

FINB_PATH="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

export PATH=$PATH:$FINB_PATH:$FINB_PATH/core
f.msg info "Located the core commands."
#echo -e "[\e[32mInfo\e[0m] Located the core commands."